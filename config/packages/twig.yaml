twig:
    paths: ['%kernel.project_dir%/templates']
    debug: '%kernel.debug%'
    strict_variables: '%kernel.debug%'
    number_format:
        decimals: 0
        decimal_point: '.'
        thousands_separator: ','
    date:
        format: 'j-n-Y'
